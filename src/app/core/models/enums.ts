export enum SortDirection {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum Role {
  CASHIER = 'CASHIER',
  SUPERVISOR = 'SUPERVISOR',
  ADMIN = 'ADMIN',
}

export enum CashRegisterStatus {
  AVAILABLE = 'AVAILABLE',
  OCCUPIED = 'OCCUPIED',
  DISABLED = 'DISABLED',
}
